mixin block2
	include data

	+b('section').section_2
		+b('div').container
			+b('div').features
				each item in features
					+b('div').feature
						+e('div').ico
							+e('span').img= item.sign
						+e('p').title= item.title
						+e('p').text= item.text
